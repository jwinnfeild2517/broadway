<nav class="navbar navbar-expand-lg navbar-light bg-light" style="background-color: #501b1d;">
  <%= link_to '17 Hundred Choices', root_path, class: "navbar-brand" %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          Genre
        </a>
        <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
         <% Category.all.order(:name).each do |category| %>
          <% params[:category] == category.name ? active_selection = "dropdown-item active" : active_selection = "dropdown-item"%>
          <li><%= link_to category.name, movies_path(category: category.name), class: active_selection %></li>
         <% end %>
        </ul>
      </li>

      <% if user_signed_in? %>
        <li class="nav-item"><%= link_to 'Add Movie', new_movie_path, class: "nav-link active"  %></li>
      <% end %>  
      
    </ul>

     <ul class="navbar-nav justify-content-end">
      <% if user_signed_in? %>
        <li class="nav-item"><%= link_to 'Log Out', destroy_user_session_path, method: :delete, class: "nav-link active"  %></li>
      <% else %>
        <li class="nav-item"><%= link_to 'Sign Up', new_user_registration_path, class: "nav-link" %></li>
        <li class="nav-item"><%= link_to 'Sign In', new_user_session_path, class: "nav-link" %></li>
      <% end %>  
    </ul>
  </div>
</nav>